# Express Dev Container Lab

## What is JSON?

JSON (JavaScript Object Notation) is a lightweight text format for representing structured data. It represents data as key–value pairs and arrays, similar to JavaScript objects. JSON is easy for humans to read and write, and easy for machines to parse and generate, so it is widely used to send data between servers and clients in web APIs.
